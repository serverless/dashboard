{
  "name": "@serverless/sdk-schema",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.cjs.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf -rf ./dist && rimraf -rf ./out && buf build && buf generate && tsc --emitDeclarationOnly && rollup --config && rimraf -rf ./dist/dts"
  },
  "author": "Serverless, Inc.",
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-multi-entry": "^4.1.0",
    "@rollup/plugin-typescript": "^8.3.3",
    "@tsconfig/node16": "^1.0.3",
    "rimraf": "^3.0.2",
    "rollup": "^2.77.2",
    "rollup-plugin-dts": "^4.2.2",
    "tslib": "^2.4.0",
    "twirpscript": "^0.0.61",
    "typescript": "^4.7.4"
  }
}
